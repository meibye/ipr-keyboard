---
applyTo: "src/backend/**,src/api/**"
---

## Backend rules
- Preserve API compatibility unless the task explicitly allows breaking changes
- Validate input and handle failures explicitly
- Prefer deterministic behavior and strong typing
- Add or update unit tests for changed business logic
- Update API or configuration docs when contracts change
