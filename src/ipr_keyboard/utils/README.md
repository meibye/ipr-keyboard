# src/ipr_keyboard/utils/

Common helper functions.

## Files

- `helpers.py`
- `__init__.py`

## Functions in `helpers.py`

- `project_root()`
- `config_path()`
- `load_json(path)`
- `save_json(path, data)`

## Notes

There is no `backend_sync.py` in current repository state. References to that module in older docs are stale.
